#ifndef DIRECTIONAL_MICROPHONE_HDR
#define DIRECTIONAL_MICROPHONE_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize directional_microphone component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t directional_microphone_init(void);

/**
 * @brief Execute directional_microphone component action
 * Called periodically by subsystem dispatcher
 */
void directional_microphone_act(void);

#endif // DIRECTIONAL_MICROPHONE_HDR
