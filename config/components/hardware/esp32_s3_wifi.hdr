#ifndef ESP32_S3_WIFI_HDR
#define ESP32_S3_WIFI_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize esp32_s3_wifi component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t esp32_s3_wifi_init(void);

/**
 * @brief Execute esp32_s3_wifi component action
 * Called periodically by subsystem dispatcher
 */
void esp32_s3_wifi_act(void);

#endif // ESP32_S3_WIFI_HDR
