#ifndef CAPACITIVE_TOUCH_SENSOR_HDR
#define CAPACITIVE_TOUCH_SENSOR_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize capacitive_touch_sensor component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t capacitive_touch_sensor_init(void);

/**
 * @brief Execute capacitive_touch_sensor component action
 * Called periodically by subsystem dispatcher
 */
void capacitive_touch_sensor_act(void);

#endif // CAPACITIVE_TOUCH_SENSOR_HDR
