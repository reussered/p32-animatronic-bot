#ifndef SERVO_MOTOR_STANDARD_HDR
#define SERVO_MOTOR_STANDARD_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize servo_motor_standard component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t servo_motor_standard_init(void);

/**
 * @brief Execute servo_motor_standard component action
 * Called periodically by subsystem dispatcher
 */
void servo_motor_standard_act(void);

#endif // SERVO_MOTOR_STANDARD_HDR
