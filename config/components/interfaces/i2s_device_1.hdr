#ifndef I2S_DEVICE_1_HDR
#define I2S_DEVICE_1_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize i2s_device_1 component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t i2s_device_1_init(void);

/**
 * @brief Execute i2s_device_1 component action
 * Called periodically by subsystem dispatcher
 */
void i2s_device_1_act(void);

#endif // I2S_DEVICE_1_HDR
