#ifndef I2S_DEVICE_2_HDR
#define I2S_DEVICE_2_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize i2s_device_2 component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t i2s_device_2_init(void);

/**
 * @brief Execute i2s_device_2 component action
 * Called periodically by subsystem dispatcher
 */
void i2s_device_2_act(void);

#endif // I2S_DEVICE_2_HDR
