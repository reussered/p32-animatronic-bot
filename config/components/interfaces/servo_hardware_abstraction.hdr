#ifndef SERVO_HARDWARE_ABSTRACTION_HDR
#define SERVO_HARDWARE_ABSTRACTION_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize servo_hardware_abstraction component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t servo_hardware_abstraction_init(void);

/**
 * @brief Execute servo_hardware_abstraction component action
 * Called periodically by subsystem dispatcher
 */
void servo_hardware_abstraction_act(void);

#endif // SERVO_HARDWARE_ABSTRACTION_HDR
