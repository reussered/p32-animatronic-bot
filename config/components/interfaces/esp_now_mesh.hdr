#ifndef ESP_NOW_MESH_HDR
#define ESP_NOW_MESH_HDR

#include <esp_err.h>
#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief Initialize esp_now_mesh component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t esp_now_mesh_init(void);

/**
 * @brief Execute esp_now_mesh component action
 * Called periodically by subsystem dispatcher
 */
void esp_now_mesh_act(void);

#ifdef __cplusplus
}
#endif

#endif // ESP_NOW_MESH_HDR
