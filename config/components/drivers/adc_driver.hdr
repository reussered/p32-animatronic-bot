#ifndef ADC_DRIVER_HDR
#define ADC_DRIVER_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize adc_driver component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t adc_driver_init(void);

/**
 * @brief Execute adc_driver component action
 * Called periodically by subsystem dispatcher
 */
void adc_driver_act(void);

#endif // ADC_DRIVER_HDR
