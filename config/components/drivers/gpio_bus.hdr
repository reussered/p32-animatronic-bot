#ifndef GPIO_BUS_HDR
#define GPIO_BUS_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize gpio_bus component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t gpio_bus_init(void);

/**
 * @brief Execute gpio_bus component action
 * Called periodically by subsystem dispatcher
 */
void gpio_bus_act(void);

#endif // GPIO_BUS_HDR
