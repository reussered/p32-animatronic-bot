#ifndef GPIO_SINGLE_DRIVER_HDR
#define GPIO_SINGLE_DRIVER_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize gpio_single_driver component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t gpio_single_driver_init(void);

/**
 * @brief Execute gpio_single_driver component action
 * Called periodically by subsystem dispatcher
 */
void gpio_single_driver_act(void);

#endif // GPIO_SINGLE_DRIVER_HDR
