#ifndef SPI_DATA_BUS_HDR
#define SPI_DATA_BUS_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize spi_data_bus component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t spi_data_bus_init(void);

/**
 * @brief Execute spi_data_bus component action
 * Called periodically by subsystem dispatcher
 */
void spi_data_bus_act(void);

#endif // SPI_DATA_BUS_HDR
