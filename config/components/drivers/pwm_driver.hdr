#ifndef PWM_DRIVER_HDR
#define PWM_DRIVER_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize pwm_driver component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t pwm_driver_init(void);

/**
 * @brief Execute pwm_driver component action
 * Called periodically by subsystem dispatcher
 */
void pwm_driver_act(void);

#endif // PWM_DRIVER_HDR
