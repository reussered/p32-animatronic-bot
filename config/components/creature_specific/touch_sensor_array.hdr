#ifndef TOUCH_SENSOR_ARRAY_HDR
#define TOUCH_SENSOR_ARRAY_HDR

#include <esp_err.h>
#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief Initialize touch_sensor_array component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t touch_sensor_array_init(void);

/**
 * @brief Execute touch_sensor_array component action
 * Called periodically by subsystem dispatcher
 */
void touch_sensor_array_act(void);

#ifdef __cplusplus
}
#endif

#endif // TOUCH_SENSOR_ARRAY_HDR
