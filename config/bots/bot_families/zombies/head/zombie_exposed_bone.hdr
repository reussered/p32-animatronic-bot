#ifndef ZOMBIE_EXPOSED_BONE_HDR
#define ZOMBIE_EXPOSED_BONE_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize zombie_exposed_bone component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t zombie_exposed_bone_init(void);

/**
 * @brief Execute zombie_exposed_bone component action
 * Called periodically by subsystem dispatcher
 */
void zombie_exposed_bone_act(void);

#endif // ZOMBIE_EXPOSED_BONE_HDR
