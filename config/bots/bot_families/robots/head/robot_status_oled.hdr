#ifndef ROBOT_STATUS_OLED_HDR
#define ROBOT_STATUS_OLED_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize robot_status_oled component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t robot_status_oled_init(void);

/**
 * @brief Execute robot_status_oled component action
 * Called periodically by subsystem dispatcher
 */
void robot_status_oled_act(void);

#endif // ROBOT_STATUS_OLED_HDR
