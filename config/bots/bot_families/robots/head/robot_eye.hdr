#ifndef ROBOT_EYE_HDR
#define ROBOT_EYE_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize robot_eye component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t robot_eye_init(void);

/**
 * @brief Execute robot_eye component action
 * Called periodically by subsystem dispatcher
 */
void robot_eye_act(void);

#endif // ROBOT_EYE_HDR
