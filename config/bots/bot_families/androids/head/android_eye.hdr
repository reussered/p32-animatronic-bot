#ifndef ANDROID_EYE_HDR
#define ANDROID_EYE_HDR

#include <esp_err.h>
#include <stdint.h>

/**
 * @brief Initialize android_eye component
 * @return ESP_OK on success, error code otherwise
 */
esp_err_t android_eye_init(void);

/**
 * @brief Execute android_eye component action
 * Called periodically by subsystem dispatcher
 */
void android_eye_act(void);

#endif // ANDROID_EYE_HDR
